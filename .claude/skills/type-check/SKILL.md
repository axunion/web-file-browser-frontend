---
description: Run type checking and build verification with `pnpm build`. Use to confirm no type errors exist.
---

Run `pnpm build` to perform type checking and build verification, then report any errors found.
