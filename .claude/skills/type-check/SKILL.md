---
description: Run type checking with `pnpm typecheck`. Use to confirm no type errors exist.
allowed-tools: Bash
---

Run `pnpm typecheck` to perform type checking (tsc --noEmit), then report any errors found. Use `pnpm build` instead when build artifact verification is also needed.
